This program solves the job scheduling problem using a greedy approach. The greedy criterion selected is
"weight-length" and the jobs are executed in decreasing order of this criterion. jobs.txt contains the
number of jobs in the first row and the weights and lengths of jobs in subsequent rows respectively.